// BAI 1
let number;
let total = 0;
while (number != -1) {
	number = +prompt("Enter a number: ");
	alert(number);
	//phần code tính tổng
	total += number;
	8;
}
document.write(total);

// BAI 2
let i = 1;
while (i < 100) {
	document.write(`<hr width = ${i}%>`);
	i++;
}
